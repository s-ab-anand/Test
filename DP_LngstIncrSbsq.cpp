#include<iostream>
#include<conio.h>
using namespace std;
/*int main()
{
    int n;
    cout<<"Enter the length of number array";
    cin>>n;
    int A[10], S[10];
    int i;
    for(i=0; i<n; i++)
    {
        cout<<" Enter the number: ";
        cin>>A[i];
        cout<<"\n";
        S[i]=1;
    }

    for(int j=0; j<n; j++)
    {
        for(int k = j+1; k<n; k++)
        {
            if(A[k]>A[j])
            {
                if(S[k]<S[j]+1)
                {
                    S[k]=S[j]+1;
                }
            }
        }

    }
    int max = S[0];
    for(int l=1 ; l<n; l++)
    {
        if(S[l]>max)
        {
            max=S[l];
        }
    }
    cout<<"\nmax="<<max;
    return 1;
}
*/
